name: Build and Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'  
  workflow_dispatch: 

jobs:
  build:
    name: Build Windows Game
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-StandaloneWindows64-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-StandaloneWindows64-
            Library-

      - name: Build project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          targetPlatform: StandaloneWindows64
          unityVersion: 6000.3.3f1
          buildMethod: Lobotom.Build.Editor.BuildCommand.BuildWindows64
          buildsPath: build
          allowDirtyBuild: true

      - name: Create ZIP archive
        run: |
          cd build/StandaloneWindows64
          zip -r ../../../Lobotom-Windows.zip .
          cd ../../..
          ls -lh Lobotom-Windows.zip

      - name: Upload to Releases
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: Lobotom-Windows.zip
          generate_release_notes: true
          name: Lobotom ${{ github.ref_name }}
          body: |
            ## Скачать игру
            - Скачайте **Lobotom-Windows.zip** ниже
            - Распакуйте архив
            - Запустите `Lobotom.exe`

      - name: Upload as artifact (для тестов)
        uses: actions/upload-artifact@v4
        with:
          name: Lobotom-Windows
          path: build/StandaloneWindows64/